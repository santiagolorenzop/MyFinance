/** Temporary working name — replace globally via this constant. */
export const APP_NAME = 'MyFinance'

export const APP_ID = 'myfinance'
export const DB_NAME = 'myfinance'
/** IndexedDB / backup schema version. Bump only with additive Dexie upgrades. */
export const SCHEMA_VERSION = 2

export const THEME_COLOR = '#1c1c1e'
export const BACKGROUND_COLOR = '#f5f5f7'

/** Near-budget warning threshold (0–1). */
export const NEAR_BUDGET_THRESHOLD = 0.8

/** Undo toast visibility window after saving an expense (ms). */
export const UNDO_TIMEOUT_MS = 5000

export const SETTINGS_ROW_ID = 'default'
